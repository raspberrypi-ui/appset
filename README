This is an application which allows the configuration of the LX Desktop user interface
from one convenient dialog.

To build, run autogen.sh, then ./configure and make.
Use sudo make install to install data files.
